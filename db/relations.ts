// Relations can be added here when needed
// Example:
// import { relations } from "drizzle-orm";
// import { users, luts } from "./schema";
//
// export const usersRelations = relations(users, ({ many }) => ({
//   luts: many(luts),
// }));
